# The ordering agent's server (friends-kitchen-agent-backend/server.py).
# The only service this app talks to — the Friends Kitchen API is reached through it.
VITE_AGENT_BASE_URL=http://localhost:8100
